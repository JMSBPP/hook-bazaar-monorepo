// SPDX-License-Identifier: MIT
pragma solidity >=0.8.30;

/**
 * @title ProtocolAdminOperator
 * @notice Implementation contract for protocol admin operators deployed via GenericFactory
 */
contract ProtocolAdminOperator {
    // This contract serves as the implementation for protocol admin operators
    // Each protocol gets its own proxy instance deployed from this implementation
    
    // Add any shared functionality here if needed
    // Currently, this is a minimal implementation for the factory pattern
}

